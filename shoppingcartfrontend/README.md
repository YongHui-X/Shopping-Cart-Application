# Shop @ISS — Frontend

React frontend module for the Shop @ISS e-commerce application. Communicates with the Spring Boot backend via REST APIs.

## Setup

```bash
npm install
npm start
```

The app runs at **http://localhost:3000** (requires the backend at http://localhost:8080).
